# =============================================================================
# Boxwatchr Rules
# =============================================================================

# Add your rules below. See rules.yaml.example for full documentation
# on all available fields, operators, actions, and the required learn field.
#
# Required fields for every rule:
#   name            A unique name for the rule.
#   match           Whether all or any conditions must match. Must be "all" or "any".
#   learn           Whether matching emails are spam or legitimate. Must be "spam" or "ham".
#   conditions      At least one condition. Each condition requires field, operator, and value.
#   actions         At least one action. Each action requires type, and destination if moving.

rules: