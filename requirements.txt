# Direct dependencies
IMAPClient==3.1.0           # IMAP connection and IDLE support
python-dotenv==1.2.2        # Load .env into environment variables
watchdog==6.0.0             # Filesystem events for rules hot-reload

# Flask and its dependencies
Flask==3.1.3                # Web dashboard
blinker==1.9.0              # Flask signals
click==8.3.1                # CLI framework (Flask dependency)
colorama==0.4.6             # ANSI color support on Windows (click dependency)
itsdangerous==2.2.0         # Secure cookie signing (Flask dependency)
Jinja2==3.1.6               # HTML templating (Flask dependency)
MarkupSafe==3.0.3           # HTML escaping (Jinja2 dependency)
Werkzeug==3.1.6             # WSGI toolkit (Flask dependency)

# requests and its dependencies
requests==2.32.5            # HTTP client for rspamd API
certifi==2026.2.25          # TLS certificate bundle (requests dependency)
charset-normalizer==3.4.6   # HTTP charset detection (requests dependency)
idna==3.11                  # Internationalized domain names (requests dependency)
urllib3==2.6.3              # HTTP connection pooling (requests dependency)

# cryptography
cryptography==46.0.5         # Fernet symmetric encryption for stored IMAP passwords

# tldextract and its dependencies
tldextract==5.3.1           # Extract domain/TLD from email addresses
filelock==3.25.2            # File locking (tldextract dependency)
requests-file==3.0.1        # file:// URL support (tldextract dependency)