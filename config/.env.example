# IMAP Settings
IMAP_HOST=imap.yourserver.com           # The hostname of your IMAP server
IMAP_PORT=993                           # IMAP port. 993 is the standard port for IMAP over SSL
IMAP_USERNAME=your@email.com            # Whatever your e-mail address is, or the portion to the left of @
IMAP_PASSWORD="yourpassword"            # Your email password wrapped in double quotes
IMAP_FOLDER=INBOX                       # The folder boxwatchr will monitor for new mail
IMAP_POLL_INTERVAL=60                   # How often boxwatchr checks for new mail in seconds, used as a fallback if IMAP IDLE is not supported
IMAP_TRASH_FOLDER=                      # The folder messages are moved to for delete actions. Leave blank to auto-detect from server (requires RFC 6154 SPECIAL-USE support).
IMAP_SPAM_FOLDER=                       # The folder messages are moved to for junk actions. Leave blank to auto-detect from server (requires RFC 6154 SPECIAL-USE support).

# Logging
LOG_LEVEL=INFO                          # How verbose the logs should be. Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
DRYRUN=true                             # While true, no IMAP actions are taken (emails are not moved or flagged). Everything else runs normally. Set to false when you are ready for boxwatchr to act on your emails.

# Spam Settings
# boxwatchr has the ability to automatically handle spam and ham emails by sending them to rspamd regardless of what rules you have in place
# To take advantage of this, use the following fields.
SPAM_LEARNING=both                      # Controls automatic rspamd learning. Options: both (submit spam and ham), spam (only submit spam), ham (only submit ham), off (no automatic learning)
SPAM_THRESHOLD=5.0                      # The rspamd score at which boxwatchr considers a message spam. 5.0 is the recommended default. Raise it if you get too many false positives, lower it to be more aggressive.
SPAM_ACTION=junk                        # What to do with messages that exceed the spam threshold. Options: junk (move to IMAP_SPAM_FOLDER), delete (move to IMAP_TRASH_FOLDER)
HAM_THRESHOLD=2.0                       # Emails scoring at or below this value are automatically submitted to rspamd as ham. Only applies when SPAM_LEARNING is "ham" or "both". Set higher to submit more emails as ham, lower to be more conservative.

# Web Dashboard Settings
WEB_PORT=8080                           # The port the boxwatchr web dashboard runs on
WEB_PASSWORD=                           # Password to access the web dashboard. Leave blank to disable password protection

# Database Settings
DB_PRUNE_DAYS=0                         # Automatically delete log entries older than this many days. Set to 0 to disable pruning (keep everything).

# rspamd Settings
RSPAMD_HOST=127.0.0.1                   # You should not need to modify this
RSPAMD_PORT=11333                       # ...or this
RSPAMD_CONTROLLER_PORT=11334            # ...or this.
RSPAMD_PASSWORD="changethispassword"    # Come up with your own password to use for rspamd's API calls